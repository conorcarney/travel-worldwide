# Copy to .env.local (never commit secrets).
ATLAS_URI=
# Optional alias if you prefer this name instead of ATLAS_URI:
# MONGODB_URI=
# Atlas database that holds Visited, Flights, etc.
MONGODB_DB=Countries
# Required for Auth.js sessions
AUTH_SECRET=
